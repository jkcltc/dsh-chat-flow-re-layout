/**
 * Browser half type declarations.
 *
 * lib/client.js is a hand-written ModuleLoader bundle: it injects the layout
 * stylesheet into <head> when the module materializes and exports an empty
 * `apply` for Cordis activation.
 */
export declare const apply: (ctx: unknown) => void
